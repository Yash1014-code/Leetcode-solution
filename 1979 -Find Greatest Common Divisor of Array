class Solution:
    def findGCD(self, nums: List[int]) -> int:
        n=min(nums)
        m=max(nums)
        for i in range(1,n+1):
            if n%i==0 and m%i==0:
                count=i
        return count        
